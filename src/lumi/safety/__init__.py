"""Deterministic, versioned, clinician-owned red-flag policy.

The model cannot invent, suppress, or override these rules: the policy's only
input is structured signals and its only output is a versioned evaluation.
"""
